# 2025-07-12 工作日志

## Selector 分层选择器模版
新建了 Apple 隐私页风格的分层选择器模版，位于 `public/webtool/` 下：
- `selector.html` — 页面入口，Sober UI shell (s-page light + s-appbar + s-scroll-view)
- `code/css/selector.css` — 样式：pill 选项按钮、面包屑、结果卡片、入场动画、light/dark 双主题
- `code/javascript/selector.js` — 逻辑 + `SELECTOR_CONFIG` 配置树

### 交互特性
- 正文流依次展示：标题 → 描述 → 选择区
- 选项卡逐层展示，点击进入下一层（基于 `children` 数组）
- 新出现的选项卡自动滚动至视口中央（`scrollIntoView({ block: 'center' })`）
- 某层仅一个选项时自动选中并继续（700ms 延迟）
- 面包屑导航可点击回退到任意层级
- 最终结果以卡片形式展示（`result.title` + `result.content` HTML）
- "重新选择"按钮重置整个选择器

### 数据结构
每个选项节点：`{ label, hint?, children? | result? }`
- `children` → 有子选项，继续下钻
- `result: { title, content }` → 最终结果
- 两者互斥

### 与项目集成
- 遵循 webtool 模式（与 status.html / credits.html 一致）
- 引用项目公共资源（pmd.js、Sober UI CDN、字体）
- 可通过修改 `SELECTOR_CONFIG` 快速适配法律/隐私/帮助等页面

### Git 提交
- commit `ae5e738` — 初版 webtool 三文件（SoberJS shell）

## Selector 模版 — Astro 框架版本重构
用户要求以 Astro 框架为主、SoberJS 为辅，避免样式不平衡。

### 新增文件
- `temple/selector.astro` — Astro 模版（FrameworkLayout + Sober UI s-card/s-button 辅助）
- `src/pages/about/legal/index.astro` — 演示页面（法律与隐私）

### 架构变化
- **主框架**：FrameworkLayout (Astro)，s-page theme="light"
- **Sober UI 辅助**：s-card（结果卡片）、s-button（重置按钮）
- **CSS/JS 注入方式**：通过 `<style slot="head" is:inline>` 和 `<script slot="scripts" is:inline type="module" define:vars={{ config }}>` 注入
- **配置传递**：`define:vars` 将 frontmatter 中的 `config` 对象序列化注入客户端脚本
- **服务端渲染**：标题和描述由 Astro 服务端渲染（`{config.title}` / `{config.description}`）
- **slot 冲突处理**：`<div style="display: contents">` 包裹层隔离 HTML slot 属性与 Astro 插槽指令

### 构建验证
- `npx astro build` 成功，`/about/legal/index.html` 正确生成（21KB）

### Git 提交
- commit `90066c7` — 修复从分支切到叶子时旧子层未隐藏的 BUG

## Selector 模版 — CSS/JS 拆分为外部文件
用户要求参照 DownloadLayout 模式，将 CSS/JS 从内联提取为外部文件，页面仅保留数据。

### 文件结构（最终）
- `src/layouts/SelectorLayout.astro` — 布局：HTML 骨架 + 引用外部 CSS/JS + 序列化 config 为 JSON
- `public/assets/code/selector.css` — 全部样式
- `public/assets/code/selector.js` — 全部交互逻辑（从 `#selector-config` JSON 读取配置）
- `src/pages/about/legal/index.astro` — 仅 config 数据 + 引用 SelectorLayout
- `temple/selector.astro` — 模版（同上，仅数据）

### 配置传递方式
从 `define:vars` 改为 `<script type="application/json" id="selector-config" set:html={JSON.stringify(config)}>`，
JS 通过 `document.getElementById('selector-config')` 读取并 `JSON.parse`。

### Git 提交
- commit `0d860fb` — 拆分 CSS/JS 为外部文件 + 新增 SelectorLayout

## Selector 模版 — 层级标题
每个选择区域上方添加标题行（带下划线分隔），用于视觉分割不同层级。

### 实现方式
- `renderLayer(options, depth, layerTitle)` 新增第三个参数
- 根层标题：`config.layerTitle`（缺省回退到 `config.title`）
- 子层标题：`option.layerTitle`（缺省回退到 `option.label`）
- CSS：`.selector-layer-title` — 小号大写文字 + border-bottom 分隔线

### Git 提交
- commit `e0221cd` — 为每个选择区域添加层级标题

## Selector 模版 — 修复移动端 toolbar 遮挡
移动端 `@media (max-width:600px)` 中 `padding: 1.5rem 1rem 3rem !important` 简写覆盖了桌面端 `padding-top: calc(var(--safezone) + 2rem)`，导致顶部间距不足避开 fixed 工具栏。
修复：在移动端媒体查询中追加 `padding-top: calc(var(--safezone, 0px) + 1.5rem) !important`。

### Git 提交
- commit `18132ef` — 修复移动端 toolbar 遮挡

## Selector 模版 — 嵌入组件版本
原有 SelectorLayout 是整页布局，无法插入其他页面。新增嵌入组件版本。

### 新增文件
- `src/components/Selector.astro` — 自包含嵌入组件，接受 `config` prop，内含 CSS/JS 引用

### JS 重构（selector.js）
- 从单实例改为多实例：扫描页面上所有 `[data-selector]` 元素
- 每个实例从自身内部 `[data-selector-config]` 读取配置，独立初始化
- 向后兼容：如果找不到 `[data-selector-config]`，回退到旧的 `#selector-config`
- 防重复初始化：`data-selector-init="true"` 标记

### CSS 变化（selector.css）
- 新增 `.selector-wrap.selector-embed` 样式（无 `.content` 覆盖，无 `min-height: 100vh`）
- 整页模式样式保持不变

### SelectorLayout 变化
- `.selector-wrap` 添加 `data-selector` 属性
- 配置脚本从外部 `#selector-config` 移入 `.selector-wrap` 内部 `[data-selector-config]`

### 用法
- 整页模式：`<SelectorLayout config={config} />`（独占页面）
- 嵌入模式：`<Selector config={config} />`（插入任意页面，支持多实例）

### Git 提交
- commit `7cf2aec` — 新增嵌入组件 + JS 多实例重构

## Selector 模版 — URL 参数同步
通过 `?selector=id:path.subpath|id2:path2` 参数控制选择器状态，支持多控件。

### 实现细节
- 页面加载时用 `framework.js` 的 `getQueryString('selector')` 读取参数（含 `URLSearchParams` 回退）
- 按 `|` 分拆多个选择器规格，按 `:` 分拆 id 和路径，按 `.` 分拆层级
- `selectPath(labels)` 方法逐层查找匹配 label 的按钮并选中
- 用户交互后 `pushState` 更新 URL（`fromURLSync` 标志抑制 URL 驱动时的重复 push）
- `popstate` 事件监听浏览器前进/后退
- 需要给 `.selector-wrap` 设置 `id` 属性才能参与 URL 同步

### 改动文件
- `public/assets/code/selector.js` — 新增 registry、syncURL、applyURLParam、selectPath
- `src/layouts/SelectorLayout.astro` — 新增 `id` prop
- `src/pages/about/legal/index.astro` — 添加 `id="legal"`
- `temple/selector.astro` — 文档补充 URL 同步说明

### Git 提交
- commit `714aedc` — URL 参数同步功能

## Selector 模版 — 命令绑定修复 + bindCommandOn
检查发现 selector.js 中动态插入元素的命令绑定有两个问题：
1. 条件反转：`!== 'function'` 时调用 executeCommand（会崩溃），`=== 'function'` 时反而 warn
2. `result.content` 分支（非 `result.html`）的 `data-cmd` 完全未绑定

### framework.js 改动
- 新增 `export function bindCommandOn(element)`：扫描元素自身及子元素的 `data-cmd`，绑定 click → executeCommand，消费后移除属性
- 初始化绑定从内联 `querySelectorAll` 改为 `bindCommandOn(document.body)`
- 导出到 `window.streack.bindCommandOn`

### selector.js 改动
- 移除重复且条件反转的内联绑定代码
- 统一在 card 构建完成后调用 `bindCommandOn(card)`，覆盖 `result.html` 和 `result.content` 两种分支

### Git 提交
- commit `49fe78d` — 新增 bindCommandOn() 并修复 selector 命令绑定
