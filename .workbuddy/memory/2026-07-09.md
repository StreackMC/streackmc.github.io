# 2026-07-09 工作日志

## DownloadLayout 模板 + 下载页面布局重排

- **新建 `src/layouts/DownloadLayout.astro`**：
  - 继承 FrameworkLayout（自动含 Toolbar + Footer + Spa）
  - 三段式内容布局：`download-hero`(版本日志+说明) → `download-actions`(按钮) → `download-meta`(淡化附加信息)
  - 透传所有 FrameworkLayout 插槽：`toolbar-nav`, `notes`, `dialogs`, `scripts`, `head`
  - `download-meta` 区域默认 opacity .5，hover 恢复到 .85，底部 gradient mask 到透明
  - SHA256 用 `font-family: monospace` + `user-select: all`，点击即全选
- **重写 `minecraft/main/s1/download.astro`**：使用 DownloadLayout 模板
  - 新增版本日志 section（发行日期 + 版本说明）
  - 新增说明文本 section
  - SHA256、空间需求(6.2GB)、Minecraft 版本要求(1.21.8) 放入淡化附加信息区
- 构建通过，sitemap 正常收录 14 个 URL
